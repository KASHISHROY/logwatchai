{
  "name": "rollback-proxy",
  "version": "1.0.0",
  "description": "Canary deployment proxy with auto-rollback",
  "main": "scripts/start-local.js",
  "scripts": {
    "start": "node scripts/start-local.js",
    "start:proxy": "npm --prefix proxy start",
    "start:stable": "npm --prefix backend-stable start",
    "start:test": "npm --prefix backend-test start",
    "start:dashboard": "npm --prefix dashboard start"
  },
  "dependencies": {
    "@pinecone-database/pinecone": "^7.1.0",
    "axios": "^1.14.0",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "http-proxy": "^1.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
